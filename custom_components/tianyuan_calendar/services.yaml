# services.yaml 完整内容
get_calendar_data:
  name: 获取月历缓存数据
  description: 从协调器内存中批量获取指定日期开始的42天历法数据，用于前端月历卡片渲染。
  fields:
    start_date:
      name: 开始日期
      description: 格式为 YYYY-MM-DD，默认为今天。
      example: "2026-06-01"
      selector:
        text: